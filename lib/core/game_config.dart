import 'package:flutter/material.dart';

class GameConfig {
  static const double arenaWidth = 1080.0;
  static const double arenaHeight = 1920.0;

  // Colors based on Void of Echoes concept
  static const Color background = Color(0xFF1A1A2E);
  static const Color darkBlue = Color(0xFF16213E);
  static const Color vibrantBlue = Color(0xFF0F3460);
  static const Color crimson = Color(0xFFE94560);
}
